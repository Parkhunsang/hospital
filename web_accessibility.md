# [Docs-02] 웹 접근성(Accessibility) 개발 지침 및 체크리스트

**PixelPerfect & A11y Studio**의 웹 표준 프로젝트는 한국 웹 콘텐츠 접근성 지침(KWCAG 2.2) 및 W3C WCAG 2.1 AA 기준을 준수합니다. 본 문서는 사용자의 신체적, 환경적 제약에 상관없이 모든 이들이 동등하게 웹사이트를 이용하도록 돕기 위한 필수 실무 지침입니다.

---

## 1. 4대 접근성 원칙 및 준수사항

### 1.1. 인식의 용이성 (Perceivable)

모든 정보와 사용자 인터페이스 구성요소는 사용자가 인식할 수 있도록 제공되어야 합니다.

- **텍스트 아님 콘텐츠의 대체 텍스트 제공**:
  - 모든 `<img>`, `<area>`, `<input type="image">`에 용도에 맞는 대체 텍스트(`alt`)를 부여합니다.
  - 아이콘이 포함된 경우, 아이콘 이미지에 대체 텍스트를 입력하거나 겉을 감싸는 컨테이너에 설명을 배치합니다.
- **멀티미디어 대체 수단**:
  - 비디오나 오디오 콘텐츠를 제공할 경우, 대본(스크립트)이나 자막을 함께 업로드해야 합니다.
- **명도 대비 (Color Contrast)**:
  - 텍스트와 배경 사이의 명도비는 **최소 4.5:1 이상**이어야 합니다. (큰 텍스트의 경우 최소 3:1)
  - 이는 저시력자나 고령자 사용자가 콘텐츠를 정상적으로 식별하도록 돕습니다.
- **색에 무관한 콘텐츠 인식**:
  - 색상만으로 정보를 전달해서는 안 됩니다. (예: "필수 입력 항목은 빨간색입니다" -> 빨간색 외에 별표(\*) 등의 기호나 텍스트를 함께 제공)

### 1.2. 운용의 용이성 (Operable)

사용자 인터페이스 구성요소와 탐색은 운용 가능해야 합니다.

- **키보드 사용 보장 (Keyboard Only)**:
  - 마우스 없이 **오직 키보드의 Tab 키와 Enter, Space, 방향키만으로** 사이트의 모든 메뉴와 기능(로그인, 검색, 링크 클릭 등)을 실행할 수 있어야 합니다.
  - 마우스를 올렸을 때만 열리는 드롭다운 메뉴는 키보드 포커스가 들어갔을 때도 열려야 합니다 (`:focus` 또는 `:focus-within` 스타일링 필요).
- **초점 이동과 표시 (Focus State)**:
  - 키보드 포커스가 위치한 요소는 브라우저 상에서 시각적으로 명확히 표시되어야 합니다.
  - 임의로 `outline: none;` 처리하여 포커스 테두리를 완전히 없애는 행동은 심각한 접근성 위반입니다. 포커스 상태를 디자인에 맞게 아름답게 커스텀해야 합니다.
  ```css
  /* Good: 포커스 상태를 예쁜 테두리로 커스텀 */
  .button:focus-visible {
    outline: 2px solid var(--color-focus);
    outline-offset: 3px;
  }
  ```
- **누르기 쉬운 영역 (Click Target)**:
  - 링크 및 버튼 등 터치/클릭 영역은 최소 **44px x 44px 이상** 크기를 유지하여, 손가락이 떨리거나 미세 조정이 힘든 사용자가 쉽게 누를 수 있도록 설계해야 합니다.

### 1.3. 이해의 용이성 (Understandable)

정보와 사용자 인터페이스의 운용은 이해하기 쉬워야 합니다.

- **기본 언어 선언**:
  - HTML 문서 최상단에 기본 언어를 명확히 표기합니다.
  ```html
  <html lang="ko"></html>
  ```
- **사용자 요구에 따른 실행 (No Surprise)**:
  - 사용자가 인지하지 못한 상태에서 새 창이 열리거나 초점이 이동되면 안 됩니다. 새 창으로 링크가 열릴 경우 반드시 이를 텍스트나 아이콘으로 사전 고지해야 합니다.
  ```html
  <a href="https://example.com" target="_blank" title="새 창 열림"
    >외부 사이트 바로가기</a
  >
  ```
- **입력 오류 방지 및 정정**:
  - 폼(Form) 입력창에 오류가 발생하면 텍스트로 명확한 이유를 제시web_accessibility.md해야 합니다. (예: 단순히 빨간 테두리만 칠하기 X -> "비밀번호는 8자 이상 입력해야 합니다" 안내 텍스트 출력)

### 1.4. 견고성 (Robust)

기술이 변화하더라도 호환성을 보장해야 합니다.

- **마크업 문법 준수**:
  - HTML 태그의 열고 닫음이 정확해야 하고, 속성의 중복 선언이 없어야 하며, 동일한 페이지 내에서 `id` 값은 유일해야 합니다.

---

## 2. 웹 접근성 향상을 위한 WAI-ARIA 기초

WAI-ARIA(Accessible Rich Internet Applications)는 HTML5만으로 설명하기 힘든 복잡한 동적 인터페이스(아코디언, 탭 메뉴, 팝업 레이어 등)의 상태와 역할을 보조 기기에 알려주는 기술입니다.

- **`role` 속성**: 태그의 실제 역할이 무엇인지 스크린 리더에 알립니다.
  - 예: `<div>`를 버튼처럼 사용해야 할 때 (지양해야 하지만 불가피할 때): `role="button"`을 명시.
- **`aria-expanded="true/false"`**: 아코디언 메뉴나 드롭다운이 열려 있는지 닫혀 있는지 상태를 알려줍니다.
- **`aria-hidden="true"`**: 디자인용 아이콘이나 장식 요소 등 스크린 리더가 소리 내어 읽을 필요가 없는 요소를 완전히 감춥니다.

---

## 3. 웹 접근성 자체 QA 체크리스트

프로젝트 개발 완료 시, 훈상 사원님은 아래의 5가지 항목을 스스로 테스트해 보고 수정 내역을 보고서로 제출해야 합니다.

1. [ ] 마우스를 뽑고 **Tab 키와 Shift + Tab 키만으로** 사이트 첫 부분부터 끝 부분까지 순서대로 이동이 가능한가?
2. [ ] 모든 이미지에 **의미에 부합하는 `alt` 대체 텍스트**가 적절하게 기입되어 있는가?
3. [ ] 키보드로 탭 이동을 할 때 **포커스가 어디에 위치했는지 눈으로 확인** 가능한가?
4. [ ] 텍스트와 배경색의 **명도비가 4.5:1 이상**을 만족하여 글씨가 뚜렷하게 보이는가?
5. [ ] 페이지 내에 HTML **문법적 오류(태그 안 닫힘, 중복 ID 등)**가 없는가?
